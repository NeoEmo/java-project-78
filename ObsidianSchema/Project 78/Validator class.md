Класс, который вызывается для вызова схемы валидации каких-либо данных

### Методы:
 1) string() - вызывает билдер схемы строки.
 2) number() - вызывает билдер схемы цифр.
 3) map() - вызывает билдер схем мап.