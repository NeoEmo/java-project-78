Фабричный класс, предоставляющий методы для создания конкретных схем: string(), number() и map(). 
Выступает точкой входа в библиотеку.

### Методы:
 1) string() - вызывает билдер схемы строки.
 2) number() - вызывает билдер схемы цифр.
 3) map() - вызывает билдер схем мап.